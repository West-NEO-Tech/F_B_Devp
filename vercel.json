{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install -g pnpm@9.15.4 && pnpm --dir development install --frozen-lockfile",
  "buildCommand": "pnpm --dir development run build",
  "outputDirectory": "development/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://f-b-devp.vercel.app/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
